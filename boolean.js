// === equality operator
// !== not equal operator
// < less than operator
// > greater than operator
// <= less than or equal to 
// >= greater than or equal to

let temp = 180

if (temp <= 32) {
    console.log('It is freezing outside')
}

if (temp >= 110 ) {
    console.log( 'It is way too hot outside ')
}

// Challenge area
// calculate is child - if they are 7 or under print message
// calculate is senior - if they are 65 or older print message

let age = 85

if (age <= 7) {
    console.log('You get a child discount.')
}

if (age >= 65) {
    console.log('You get a senior citizen discount.')
}
